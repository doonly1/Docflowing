# 生产环境锁定版本（pip install -r requirements-lock.txt）
# 推荐用此文件进行桌面打包构建，确保依赖版本稳定可复现

# 文档处理
python-docx==1.1.0
python-pptx==1.0.2
openpyxl==3.1.2
pdfplumber==0.11.4
beautifulsoup4==4.12.3

# AI 文本抽取（[docx] extra 提供 mammoth/lxml，docx 转换必需；
# 表格 xlsx/xls 不做内容转换，仅文件名索引，故不需要 pandas/xlrd）
markitdown[docx]==0.1.3

# Web 框架
flask==3.0.3
flask-cors==4.0.1
werkzeug==3.0.3

# 配置与网络（cryptography 已移除，P2P 签名改用标准库 hmac）
pyyaml==6.0.2
requests==2.32.3
zeroconf==0.132.2

# 桌面壳（pywebview + 系统托盘）
pywebview==5.3.1
pystray==0.19.5
Pillow==10.4.0
# pythonnet 提供 .NET CLR 桥接，pywebview edgechromium 后端必需（打包时需收集 runtime DLL）
pythonnet==3.1.0; sys_platform == "win32"

# 测试
pytest==8.3.2
pytest-cov==5.0.0

# Windows 可选依赖（提供 Word COM 支持，其他平台不需要）
pywin32==306; sys_platform == "win32"
docx2pdf==0.1.8; sys_platform == "win32"
